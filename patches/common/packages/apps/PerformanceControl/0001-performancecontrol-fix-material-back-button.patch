From f2e3ba2a8a68b8ad015d1e00ba198434e7b53967 Mon Sep 17 00:00:00 2001
From: Meticulus <theonejohnnyd@gmail.com>
Date: Sat, 30 Jul 2016 07:36:47 -0500
Subject: [PATCH] performancecontrol: fix material back button

Change-Id: Ie6d6767f84eb950683917af94e70919c911a46b0
---
 .../performance/activities/MainActivity.java        | 21 +++------------------
 1 file changed, 3 insertions(+), 18 deletions(-)

diff --git a/src/com/brewcrewfoo/performance/activities/MainActivity.java b/src/com/brewcrewfoo/performance/activities/MainActivity.java
index 12a56de..087c8cf 100644
--- a/src/com/brewcrewfoo/performance/activities/MainActivity.java
+++ b/src/com/brewcrewfoo/performance/activities/MainActivity.java
@@ -169,17 +169,10 @@ public class MainActivity extends Fragment implements Constants, ActivityThemeCh
         if (container instanceof PreferenceFrameLayout) {
             ((PreferenceFrameLayout.LayoutParams) rootView.getLayoutParams()).removeBorders = true;
         }
-
         checkForSu();
 
         return rootView;
-    }
-
-    @Override
-    public void onActivityCreated(Bundle savedInstanceState) {
-        super.onActivityCreated(savedInstanceState);
-        setHasOptionsMenu(true);
-    }
+    }*/
 
     @Override
     public void onConfigurationChanged(Configuration newConfig) {
@@ -198,22 +191,14 @@ public class MainActivity extends Fragment implements Constants, ActivityThemeCh
     }
 
     @Override
-    public void onCreateOptionsMenu(Menu menu, MenuInflater inflater) {
-        inflater.inflate((mIsTabbed ? R.menu.menu_tabbed : R.menu.menu_drawer), menu);
-        if (!getResources().getBoolean(R.bool.config_allow_toggle_tabbed)) {
-            menu.removeItem(R.id.pc_action_tabbed);
-        }
-        restoreActionBar();
-        super.onCreateOptionsMenu(menu, inflater);
-    }
-
-    @Override
     public boolean onOptionsItemSelected(MenuItem item) {
+	/*
         if (!mIsTabbed) {
             if (mDrawerToggle.onOptionsItemSelected(item)) {
                 return true;
             }
         }
+	*/
         switch (item.getItemId()) {
             case android.R.id.home:
                 Intent homeIntent = new Intent();
-- 
1.9.1

